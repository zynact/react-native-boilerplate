# Hoist all packages to node_modules root – required for React Native's metro bundler
# and native module linking to work correctly with pnpm
strict-peer-dependencies=true
auto-install-peers=true
node-linker=hoisted
shamefully-hoist=true
legacy-peer-deps=true
public-hoist-pattern[]=*react-native*

