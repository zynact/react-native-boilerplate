// Shared hooks barrel – export hooks here as they are created
export { useApiError } from './useApiError';
export { useNetworkStatus } from './useNetworkStatus';
export type { NetworkStatus } from './useNetworkStatus';
